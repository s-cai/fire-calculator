#!/bin/sh

# Run TypeScript type checking before commit (matches build check)
echo "🔍 Running TypeScript type check..."
npm run typecheck

# If typecheck fails, abort commit
if [ $? -ne 0 ]; then
  echo "❌ TypeScript errors found. Please fix them before committing."
  exit 1
fi

echo "✅ TypeScript check passed"

